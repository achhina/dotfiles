#!/usr/bin/env bash
# Direnv configuration for dotfiles repository

# Use the flake's default devShell
use flake

# Watch for changes in flake.nix and nix files
watch_file flake.nix
watch_file nix/**/*.nix
